// import { fromJS } from 'immutable';
// import { makeSelectCommentPageDomain } from '../selectors';

// const selector = makeSelectCommentPageDomain();

describe('makeSelectCommentPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
