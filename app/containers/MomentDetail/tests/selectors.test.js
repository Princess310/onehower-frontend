// import { fromJS } from 'immutable';
// import { makeSelectMomentDetailDomain } from '../selectors';

// const selector = makeSelectMomentDetailDomain();

describe('makeSelectMomentDetailDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
