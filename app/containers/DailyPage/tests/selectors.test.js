// import { fromJS } from 'immutable';
// import { makeSelectDailyPageDomain } from '../selectors';

// const selector = makeSelectDailyPageDomain();

describe('makeSelectDailyPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
