// import { fromJS } from 'immutable';
// import { makeSelectUserCardDomain } from '../selectors';

// const selector = makeSelectUserCardDomain();

describe('makeSelectUserCardDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
