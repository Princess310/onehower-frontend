// import { fromJS } from 'immutable';
// import { makeSelectLifePageDomain } from '../selectors';

// const selector = makeSelectLifePageDomain();

describe('makeSelectLifePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
